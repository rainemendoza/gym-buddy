import TopMenu from '@/components/TopMenu';
import AboutSection from '@/components/AboutSection';
import './style.css';

/** The Home page. */
const Home = () => (
  <main>
    <TopMenu />
    <AboutSection />
  </main>
);

export default Home;
